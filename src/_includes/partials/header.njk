<header>
    <nav>
    {%- for entry in collections.all | eleventyNavigation %}
      <a class="nav-item{% if entry.url == page.url %} nav-item-active{% endif %}" href="{{ entry.url | url }}">{{ entry.title }}</a>
    {%- endfor %}
    </nav>
</header>